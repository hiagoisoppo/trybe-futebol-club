import { Router } from 'express';

const router = Router();

// router.use('/teams', );
// router.use('/login', );
// router.use('/matches', );
// router.use('/leaderboard', );

export default router;
